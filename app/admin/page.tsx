"use client";import{useState}from"react";
export default function Admin(){const[key,setKey]=useState(""),[difficulty,setDifficulty]=useState("کارآگاه"),[busy,setBusy]=useState(false),[msg,setMsg]=useState(""),[created,setCreated]=useState<any>();
async function generate(){setBusy(true);setMsg("");setCreated(null);try{const r=await fetch("/api/admin/generate-case",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({key,difficulty})}),j=await r.json();if(!r.ok)throw Error(j.error||"خطا");setCreated(j.case);setMsg(j.message)}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}
return <main className="adminPage"><div className="adminShell"><div className="tag">PARWANDE · CONTROL ROOM</div><h1>پنل مدیریت 🕵️</h1><p className="muted">ساخت پرونده AI با Validator قبل از ورود به دیتابیس.</p><label>کلید مدیریت<input value={key} onChange={e=>setKey(e.target.value)} type="password" placeholder="ADMIN_KEY"/></label><label>درجه سختی<select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>آسان</option><option>کارآگاه</option><option>سخت</option><option>نابغه</option></select></label><button className="btn" disabled={busy||!key} onClick={generate}>{busy?"در حال ساخت و بررسی…":"ساخت پرونده جدید ✨"}</button>{msg&&<div className={"adminMsg "+(created?"ok":"")}>{msg}</div>}{created&&<div className="created"><b>{created.title}</b><span>کد پرونده: #{created.code}</span></div>}<div className="adminInfo"><b>Pipeline</b><span>AI Generate → Validator → Supabase → Ready</span></div></div></main>}