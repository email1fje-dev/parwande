"use client";
import {useEffect,useState} from "react";
const story="ساعت ۲۳:۴۰ بود. پلیس تماسی اضطراری از یک عمارت قدیمی دریافت کرد. وقتی مأموران رسیدند، پنج نفر داخل ساختمان بودند و هیچ‌کدام حاضر نبودند تمام حقیقت را بگویند. برق ساختمان چند دقیقه قطع شده بود و در همین فاصله اتفاقی جدی رخ داده بود. حالا پرونده در اختیار توست. باید با بررسی شواهد، پرسیدن سؤال‌های درست و پیدا کردن تناقض‌ها بفهمی چه اتفاقی افتاده است.";
export default function Home(){const [i,setI]=useState(0);const [done,setDone]=useState(false);useEffect(()=>{if(i<story.length){const t=setTimeout(()=>setI(i+1),32);return()=>clearTimeout(t)}setDone(true)},[i]);return <main className="page"><section className="case"><div className="tag">CASE FILE #001 · محرمانه</div><h1 className="title">پرونده</h1><div className="subtitle">یک حقیقت پنهان شده. پیدا کردنش با توست.</div><div className="meta"><span className="pill">۵ مظنون</span><span className="pill">پرونده قابل حل</span><span className="pill">درجه سختی: کارآگاه</span></div><p className="story">{story.slice(0,i)}{!done&&<span className="cursor">&nbsp;</span>}</p><div className="actions"><button className="btn" onClick={()=>{if(!done){setI(story.length);setDone(true)}else alert("محیط تحقیقات در نسخه بعدی متصل می‌شود.")}}>{done?"شروع تحقیقات":"رد کردن انیمیشن"} ▶</button></div></section></main>}
